extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    article.grid-container.portfolio-intro
        .grid-x
        .cell.small-6.medium-9.large-9
            h1 Ops! Sorry, There is a problem!
            i (#{error.status} - #{error.message})
            p You can learn about this kind of error here: 
                a(href="https://teamtreehouse.com/library/error-handling-middleware") Error Handling Middleware
                pre= error.stack